# Uncomment and set when deploying frontend/backend to different origins.
# In local dev, leave this unset -- Vite's proxy (vite.config.js) forwards
# /api requests to http://localhost:5000 automatically.
# VITE_API_BASE_URL=https://your-backend.example.com/api
